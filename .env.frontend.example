NEXT_PUBLIC_API_URL=http://localhost:3000
# Used by Server Components inside Docker — routes to the backend container directly.
# In non-Docker environments this can be left unset; NEXT_PUBLIC_API_URL is used as fallback.
INTERNAL_API_URL=http://backend:3000
NEXT_PUBLIC_DEFAULT_LOCALE=vi
PORT=5000
